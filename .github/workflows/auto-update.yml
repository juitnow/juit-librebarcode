name: Automatic Updates

on:
  schedule:
    - cron: "15 0 * * 5"
  workflow_dispatch:

jobs:
  auto-update:
    name: Automatic Updates
    uses: juitnow/workflows/.github/workflows/auto-update.yml@main
    secrets: inherit
